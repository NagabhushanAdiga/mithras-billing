# Copy to .env.production before building for GoDaddy (or any static host).
# Replace with your live backend URL (must include /api path).

VITE_API_URL=https://your-domain.com/api

# Optional: mobile barcode scanner relay (if you run scanRelay)
# VITE_SCANNER_RELAY_URL=wss://your-domain.com/scan
